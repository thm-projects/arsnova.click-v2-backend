stages:
  - build
  - test
  - deploy

build:
  stage: build
  only:
    - master
    - fixPipeline
  tags:
    - nodejs
  script:
    - npm install
    - npm run build:PROD
  artifacts:
    paths:
      - dist

test:
  stage: test
  only:
    - master
    - fixPipeline
  tags:
    - nodejs
  script:
    - npm install
    - npm test

deploy:
  stage: deploy
  only:
    - master
    - fixPipeline
  tags:
    - ssh
  dependencies:
    - build
  script:
    - chmod -R a+rX,ug+w .
    - eval $(ssh-agent -s)
    - mkdir ~/.ssh
    - ssh-keyscan "$STAGING_BACKEND_URL" >> ~/.ssh/known_hosts    
    - ssh-add <(echo "$STAGING_SSH_PRIVATE_KEY")
    - echo "test"
    - scp -r dist/* "$STAGING_BACKEND_SSH_URL"

